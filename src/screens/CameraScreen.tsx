import { AppScreen } from '../components';

export function CameraScreen() {
  return (
    <AppScreen
      title="Camera"
      description="Camera capture flow placeholder ready for future implementation."
    />
  );
}
